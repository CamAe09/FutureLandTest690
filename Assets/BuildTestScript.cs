using UnityEngine;

// Simple test script to trigger compilation and check for errors - UPDATED
public class BuildTestScript : MonoBehaviour
{
    void Start()
    {
        Debug.Log("Build test script - compilation successful - UPDATED");
    }
}